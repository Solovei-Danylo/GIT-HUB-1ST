def format_string(string, length):
    if string >= length:
        return f"{string}"
    else:
        string <= length
        string = (length - len(string)) // 2
        return f"{length}"


format_string(10, 5)
print(format_string)
